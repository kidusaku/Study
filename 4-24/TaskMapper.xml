<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.TaskMapper">
	<!-- タスク情報 全検索 -->

	<select id="findAll" resultType="com.example.demo.entity.TaskEntity">
		SELECT
			task_id,
			task_name,
			task_status,
			start_date,
			end_date
		FROM
			task
			ORDER BY task_id
	</select>
	
	
	<select id="getOne" resultType="com.example.demo.entity.TaskEntity">
		SELECT
			task_id,
			task_name,
			task_status,
			start_date,
			end_date
		FROM
			task		
		WHERE task_id = #{taskId}
		ORDER BY task_id
	</select>
	
	<insert id="taskSave" parameterType="com.example.demo.entity.TaskEntity">
	INSERT INTO task(
	task_name,
	task_status,
	start_date
	) VALUES (
	    #{taskName},
	    #{taskStatus},
	    #{startDate}
	)
	</insert>
	
	<update id="taskUpdate" parameterType="com.example.demo.entity.TaskEntity">
	UPDATE task
	SET
  	  task_name = #{taskName},
  	  task_status = #{taskStatus}
	WHERE 
		task_id = #{taskId}
	</update>
	
	<delete id="taskDelete" parameterType="com.example.demo.entity.TaskEntity">
		DELETE FROM
	    task
	WHERE
	    task_id = #{taskId}
	</delete>
	
	<update id="taskEnd" parameterType="com.example.demo.entity.TaskEntity">
	UPDATE task
	SET
  	  end_date = #{endDate}
	WHERE 
		task_id = #{taskId}
	</update>
	
	
</mapper>